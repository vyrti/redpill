[package]
name = "redpill-agent"
version = "0.1.0"
edition = "2021"
description = "Claude Code CLI integration for RedPill"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-channel = "2"
thiserror = "2"
tracing = "0.1"
